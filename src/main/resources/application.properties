spring.application.name=Spring-JPA
server.port=7675

# Database connection
spring.datasource.url=jdbc:postgresql://localhost:5432/hospital
spring.datasource.username=postgres
spring.datasource.password=sai
spring.datasource.driver-class-name=org.postgresql.Driver
# Hibernate schema management
spring.jpa.hibernate.ddl-auto=create
# ? Keep schema, don?t drop data
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# SQL logging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Context path
server.servlet.context-path=/api/v1

# Disable data.sql script since data already exists
# spring.jpa.defer-datasource-initialization=true
# spring.sql.init.mode=always
# spring.sql.init.data-locations=classpath.data.sql

#security config

#spring.security.user.name = sai
#spring.security.user.password=sai

jwt.secretKey=gcqouvcytie2r8f8ouwtvyfwdvxiufyctxc867r786cc7

